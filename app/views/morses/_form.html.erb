<%= form_for(@morse) do |f| %>
  <% if @morse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@morse.errors.count, "error") %> prohibited this morse from being saved:</h2>

      <ul>
      <% @morse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_field :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
